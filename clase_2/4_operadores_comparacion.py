# Operadores de comparación

# Se utilizan para comparar dos valores y devolver un bool (True o False)

# Los operadores de comparación son:
# == Igual a
# != Distinto a
# > Mayor que
# < Menor que
# >= Mayor o igual que
# <= Menor o igual que 